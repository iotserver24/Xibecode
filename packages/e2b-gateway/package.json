{
  "name": "@xibecode/e2b-gateway",
  "version": "1.3.3",
  "private": true,
  "type": "module",
  "description": "Team-hosted gateway for running XibeCode commands on E2B sandboxes",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@e2b/code-interpreter": "^1.5.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
